name: Test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node --check app.js
      - run: node --check scripts/monitor-sources.mjs
      - run: node --check scripts/validate-monitor-output.mjs
      - run: node tests/core.test.mjs
      - run: node tests/reliability.test.mjs
      - run: node tests/data-integrity.test.mjs
      - run: node tests/source-monitor.test.mjs
